/**
 * Main barrel export for API client
 * Re-exports everything from api and hooks
 */

// Export all API types, clients, and utilities
export * from "@/api";

// Export all React Query hooks
export * from "@/hooks";

